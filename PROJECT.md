# 项目简介

**一个用中文对话就能发邮件的机器人管家。**

你对它说「给我自己发封邮件，主题是周末安排，说周六上午十点碰面」，
它就把邮件发出去。核心是用 **MCP 协议**把邮件能力封装成标准工具，
再由**本地大模型**理解你的意图并调用这些工具。

---

## 它解决什么问题

发邮件本身不难，难的是把它接进自动化流程，并且**保证不乱发、不重发、失败可见**。

这个项目把「发邮件」做成符合 MCP 规范的服务，于是任何支持 MCP 的客户端
（或你自己的脚本、智能体）都能直接调用，而不必各自重复实现 SMTP 那套细节。

同时它把邮件发送中那些**容易被忽略却会真实出事**的环节都处理了：

| 真实问题 | 本项目的处理 |
|---|---|
| 网络抖动导致发送失败 | 区分瞬时/永久错误，只对可重试的重试 |
| 重试造成重复邮件 | 提供幂等键，同键只发一次 |
| 附件路径写错却"发送成功" | 附件不存在就**中止发送**并明确报错 |
| 文件名说错就发不出去 | 按主干唯一匹配自动修正，并告知替换了什么 |
| 附件太大只能等服务器报错 | 按编码后体积预检，发送前就拒绝 |
| 每封信重新握手，慢 | 连接复用，实测省约 328ms/封 |
| "发送成功"其实是猜的 | 可选用 IMAP 回读确认，且**如实标注**它能证明什么 |

---

## 架构

```
你 ──说人话──► 邮件管家 ──MCP 协议──► 邮件工具 ──SMTP──► QQ 邮箱
              (本地 Ollama)      (标准工具协议)
```

服务端基于 **FastAPI + 官方 MCP Python SDK 的 Streamable HTTP 传输层**实现，
不是手搓的 JSON-RPC——因此任意合规 MCP 客户端都能接入。

---

## 技术要点

以下每一条都有对应的实现与测试，不是概念罗列。

### MCP 协议的完整实现

支持完整的 `initialize` 握手与会话生命周期（`POST /mcp` → `GET`(SSE) → `DELETE`）。
早期版本曾自行实现一个只处理 `tools/list` 与 `tools/call` 的假端点，
缺少握手，导致合规客户端连不上——后来为了绕开它甚至加了一个直连后门端点，
使"邮件发送"根本不经过 MCP。现已彻底重构为官方传输层。

### SMTP 连接复用

`smtplib` 的连接不能跨线程共享，而发送经 `asyncio.to_thread` 执行，
因此"线程本地连接"也保证不了复用。方案是**一个专用工作线程独占一条连接 + 队列串行化**：

| | 每封新建连接 | 连接复用 |
|---|---|---|
| 5 封邮件建立连接数 | 5 | **1** |
| 平均每封耗时 | 906 ms | **578 ms** |

串行化是刻意接受的代价：单账号下并发发送只会更快触发服务端限流。

### 区分瞬时与永久故障

| 类别 | 例子 | 处理 |
|---|---|---|
| 瞬时（可重试） | `421`/`450`/`451`/`452`、连接断裂、超时 | 按策略重试，1s→2s 退避 |
| 永久（不重试） | `550` 收件人不存在、`535` 认证失败 | 立即失败，不浪费配额 |

⚠️ 认证失败**永不重试**——`SMTPAuthenticationError` 是 `SMTPResponseException`
的子类，判定顺序必须让永久错误优先（有测试固化这一点）。

### 重试的安全边界

SMTP 是 **at-least-once** 语义，盲目重试会重复发信：

- 错误发生在连接/登录/MAIL/RCPT 阶段 → 能确定服务器未接收，重试安全
- 断开发生在正文传输之后 → 服务器可能已收下，重试会重复

因此连接断裂时是否重连重试由 `SEND_RETRY_ON_RECONNECT` 控制，默认开启
（更常见的故障是空闲连接在传正文前被回收）。这个取舍无法完全消除，
项目选择**把它写明**而不是假装不存在。

### 幂等键

调用发送工具时传入 `idempotency_key`，同一键在有效期内只真正发送一次：

- 重复请求返回首次结果并标记 `duplicate: true`
- 首次仍在处理中时返回"处理中"，不会重复发送
- 首次因**永久错误**失败会**释放**键，允许修正后重试
- 被幂等拦截的请求**不计入**发送总数，避免污染成功率等派生指标

### 附件处理

**三条容易踩的规则的实现**：

1. **附件不存在 → 中止发送**。早期是静默跳过，结果是用户以为附件发出去了，
   对方实际只收到一封空邮件。这类静默失败极难察觉。
2. **文件名说错 → 尝试修正并告知**。小模型常猜错扩展名（实测：目录里是
   `示例报表.csv`，模型给出 `示例报表.xlsx`）。按「主干一致 → 归一化一致」
   逐级匹配，**候选不唯一时宁可报错也不猜**。
3. **大小预检按编码后体积**。Base64 每 3 字节变 4 字节，按原始大小比较会低估
   实际占用。默认单件 10MB / 合计 18MB（编码后约 24MB，低于 QQ 的约 25MB 上限）。

### 发送确认（IMAP 回读）

可选地在发送后用 IMAP 回读 QQ 的「已发送」文件夹。**这里刻意做了诚实的界定**：

- **能证明**：QQ 接收了并归档了
- **不能证明**：收件人真的收到了

实测发现被 SMTP 以 550 拒绝的邮件**同样会出现在「已发送」里**，
所以它**不是**投递确认。项目在返回值与文档中都如实标注结论强度，
而不是含糊地说"已投递"。

### 可观测性

- **指标**：成功率、延迟分位、失败原因分布，`/metrics` 暴露
- **`connects_per_send`**：验证连接复用是否生效的关键指标（远小于 1 即为复用）
- **结构化日志**：`JSON_LOGS=1` 输出单行 JSON

### 测试

**243 个用例，全程离线**（不联网、不碰真实邮箱、约 3.5 秒跑完）。

隔离手段：替换 `smtplib.SMTP_SSL` 与 `imaplib.IMAP4_SSL` 为记录型替身；
用 `httpx.ASGITransport` 在进程内驱动真实 MCP 协议栈。

**测试抓出过的真实缺陷**（都能用测试复现）：

| 缺陷 | 症状 |
|---|---|
| 懒加载连接池无锁 | 8 并发建立了 8 条本应唯一的连接 |
| `SMTPException` 继承 `OSError` 被误吞 | 所有协议错误都误判为连接断裂 |
| `_last_used` 初值 0.0 | 一次失败后必定重连 |
| 幂等命中污染发送计数 | 成功率等指标失真 |
| 多轮对话无历史 | 「主题改成…」完全接不住 |
| 提示词留了"可以先问一下"的余地 | 模型只反问，一封邮件都发不出去 |
| 附件缺失静默成功 | 对方收到没附件的邮件 |
| 时间戳精度只到秒 | 同一秒的两条记录"删一条少两条" |

---

## 适用与不适用

**适合**

- 用自然语言发一两封邮件（管家）
- 脚本/定时任务发通知（带幂等键，避免重试导致重复）
- 学习 MCP 协议的实际集成
- 需要一个"发送可靠、失败可见"的邮件发送层

**不适合**

- **批量群发**：QQ 对单账号有频率限制，触发后返回 `550 Too many attempts`，需等数小时
- 收取邮件：只能发。IMAP 仅用于「已发送」回读确认
- 回复/转发：未实现 `In-Reply-To` / `References`
- 非 QQ 邮箱：SMTP/IMAP 地址与授权码方式都是 QQ 特有的

---

## 项目状态

| 项 | 数值 |
|---|---|
| 源码 | 13 个文件，约 3,200 行 |
| 测试 | 243 个用例，约 3,400 行，全程离线 |
| 邮件工具 | 4 个 |
| HTTP 端点 | 5 个 |
| 已知正确性问题 | **无** |

---

## 文档导航

| 文档 | 内容 |
|---|---|
| **`MANUAL.md`** | **使用手册** —— 从零到发信的操作步骤、故障排查 |
| `README.md` | 设计文档 —— 架构、设计决策与取舍、协议细节 |
| `PROJECT.md` | 本文件 —— 项目简介 |
