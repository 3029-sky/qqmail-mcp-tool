# 项目速览

> 一页看完。适合面试前复习或作为简历附页。
> 完整说明见 [`README.md`](README.md)，设计决策与缺陷复盘见 [`DESIGN.md`](DESIGN.md)。

---

## 一句话

**用中文跟它说话，它就把邮件发出去。** 用 MCP 协议把邮件能力封装成标准工具，
由本地大模型理解意图并调用。附带一个能直接粘贴图片的图形界面。

---

## 为什么做这个

发一封邮件调用 `smtplib` 就够了。真正麻烦的是**让它可靠**：

- 附件路径写错，程序回一句「发送成功」，对方却收到一封空邮件 —— 双方都以为正常
- 网络抖一下重试，结果发了三遍
- 批量发给几十个人，把当天配额打光
- 「发送成功」到底是真发出去了，还是程序在猜

这个项目把「发邮件」做成符合 MCP 规范的服务，同时把上面这些**容易被忽略
却会真实出事**的环节都处理掉。任何支持 MCP 的客户端都能直接调用它。

---

## 关键数字（都是实测，不是估算）

| 项 | 数值 | 怎么测的 |
|---|---|---|
| SMTP 连接复用 | 5 封邮件 **1** 条连接，省 **328ms/封**（-36%） | 真实 QQ SMTP 连发 5 封对比 |
| 连接数指标 | `/metrics` 的 `connects_per_send` 远小于 1 即复用生效 | 同一接口自证 |
| 本地模型响应 | 4–10 秒/轮 | `qwen2.5:3b`，CPU 推理 |
| DeepSeek 响应 | **1.3 秒/轮** | 同机实测对比 |
| 测试套件 | **507 个用例，全程离线，约 20 秒** | `pytest -q` |
| 附件限制 | 单件 10MB / 合计 18MB（按**编码后**体积判断） | Base64 膨胀 4/3 |

---

## 技术要点

每一条都有实现与测试，不是概念罗列。**追问细节时按此展开。**

### 1. 协议正确性

用**官方 MCP Python SDK 的 Streamable HTTP 传输层**，支持完整的
`initialize` 握手与会话生命周期。

> 早期版本自己写了个只处理 `tools/list` / `tools/call` 的端点，
> 没有握手，合规客户端连不上；当时为了绕开它甚至加了个直连后门，
> 结果**核心功能根本不经过 MCP**。现已删除后门、重写为官方传输层。
>
> 一个细节：官方传输层是直接读写 ASGI `scope/receive/send` 的原生应用，
> 不能注册成 FastAPI 路径函数（Starlette 会把返回值再序列化一次，导致 422），
> 必须以原生 ASGI 形式挂载。

### 2. SMTP 连接复用的取舍

`smtplib` 连接不能跨线程共享，而发送经 `asyncio.to_thread` 执行。
方案是**一个专用工作线程独占一条连接 + 队列串行化**。

串行化是刻意接受的代价：单账号下并发发送只会更快触发限流，
而连接复用省下的握手成本更实在。**要提吞吐的正确方向是多账号分片，
不是给单连接加锁。**

### 3. 区分「能重试」与「不能重试」

| 类别 | 例子 | 处理 |
|---|---|---|
| 瞬时 | `421/450/451/452`、连接断裂、超时 | 1s→2s 退避重试 |
| 永久 | `550` 收件人不存在、`535` 认证失败 | 立即失败，不浪费配额 |

坑：`SMTPAuthenticationError` 是 `SMTPResponseException` 的子类，
而 `SMTPException` 又继承 `OSError` —— 判定顺序错一点，
所有协议错误都会被当成「连接断裂」而重试。

### 4. 重试的安全边界（诚实说明）

SMTP 是 **at-least-once** 语义，盲目重试会重复发信：

- 错误发生在连接/登录/MAIL/RCPT 阶段 → 服务器确定未接收，重试安全
- 断开发生在正文传输**之后** → 服务器可能已收下，重试会重复

这个取舍**无法完全消除**，所以项目选择用 `SEND_RETRY_ON_RECONNECT`
把它交给使用者决定，并写进文档——而不是假装不存在。

### 5. 幂等键

同一键在有效期内只真正发送一次：重复请求返回首次结果并标 `duplicate: true`；
首次因**永久错误**失败会释放键，允许修正后重试；
被幂等拦截的请求**不计入发送总数**，避免污染成功率。

### 6. 附件处理的三条硬规则

1. **找不到就中止发送**（不静默跳过）
2. **名字说错就尝试修正并告知**（主干 → 归一化逐级匹配，**候选不唯一时宁可报错也不猜**）
3. **大小按编码后体积预检**（Base64 让体积膨胀 1/3）

> 有个反直觉的边界：**指明了目录但文件不存在时，绝不能转头去附件目录
> 搜同名文件**。否则「发 `D:\报表\a.txt`」会变成「发附件目录里的另一份
> a.txt」——发送成功、附件名也对，但**发错了文件**。

### 7. 剪贴板导入（终端版）

终端**粘不出图片，也粘不出文件路径**（实测：复制文件后剪贴板里
只有 `CF_HDROP`，没有文本格式）。所以直接读剪贴板：

- 复制的文件 → `CF_HDROP` 拿到路径
- 截图/网页图片 → `CF_DIB` 是**裸位图数据**，需转换

转换刻意不引入 Pillow：`CF_DIB → 内存拼 PPM → tkinter.PhotoImage → PNG`，
只用标准库。位图解析里三处最容易**静默**出错的地方（行按 4 字节对齐、
自下而上存储、BGRA 通道顺序）都有逐像素测试——错了不抛异常，
只会发出一张歪的或红蓝颠倒的图。

### 8. 可观测性

- 成功率、延迟分位、失败原因分布，经 `/metrics` 暴露
- `connects_per_send` 是验证连接复用的关键指标
- `JSON_LOGS=1` 输出单行 JSON，便于日志系统采集

---

## 工程实践

| 做法 | 具体体现 |
|---|---|
| **失败要可见** | 附件缺失、批量地址非法都**中止**而非跳过；宁可整批失败也不静默少发 |
| **文档与代码一致** | 有一条 bug 就是「文档教用户改 `.env`，代码却把它写死」；现在有测试比对二者 |
| **安全默认值** | 图形界面只监听 `127.0.0.1`；凭据只经 `.env`，源码零硬编码（有测试固化） |
| **凭据不回显** | 配置接口只回「是否已设置」与长度，界面永远拿不到授权码内容 |
| **测试隔离** | 507 个用例不联网、不碰真实邮箱；并有测试专门防止「测试写进真实目录」 |
| **界面与后端分离** | 两个界面共用 `butler_core.py`，避免提示词/历史逻辑在两边漂移 |

**11 条真实缺陷的完整复盘见 [`DESIGN.md`](DESIGN.md)** ——
那部分是最能体现工程判断力的内容。

---

## 适合 / 不适合

**适合**

- 用自然语言发一两封邮件
- 脚本或定时任务发通知（带幂等键，避免重试导致重复）
- 学习 MCP 协议的实际集成
- 需要一个「发送可靠、失败可见」的邮件发送层

**不适合**

- **批量群发**：QQ 单账号有频率限制，触发后需等数小时（项目因此把批量上限
  与间隔做成**不可绕过的安全边界**）
- 收邮件：只能发，IMAP 仅用于「已发送」回读确认
- 回复/转发：未实现 `In-Reply-To` / `References`
- 非 QQ 邮箱：SMTP/IMAP 地址与授权码方式都是 QQ 特有的

---

## 项目状态

| 项 | 数值 |
|---|---|
| 源码 | 21 个 Python 文件，约 6,400 行 + 前端 `static/index.html` 约 2,200 行 |
| 测试 | **507 个用例**，约 6,800 行，全程离线，约 20 秒 |
| 邮件工具 | 4 个（批量发送复用同一套工具，不额外暴露入口） |
| HTTP 端点 | `/mcp`、`/health`、`/metrics`、`/tools` |
| 界面 | 图形界面 + 终端界面，共用后端 |
| 模型 | 本地 Ollama（默认 `qwen2.5:3b`）或 DeepSeek 云端，可切换 |
| 已知正确性问题 | **无** |
