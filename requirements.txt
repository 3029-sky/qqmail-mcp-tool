mcp>=1.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
aiosmtplib>=2.0.0
email-validator>=2.1.0
python-dotenv>=1.0.0
rich>=13.0.0
python-multipart>=0.0.6

# 剪贴板读取（clipboard.py 的 /粘贴 指令）。
# 它本来是 mcp 的间接依赖，但我们是直接 import 的，所以显式声明。
# 只在 Windows 上安装；其他平台上 /粘贴 会提示不可用，不影响发信。
pywin32>=306; sys_platform == "win32"

# 邮件管家与智能体（email_butler.py / webui.py）
# langchain-ollama 提供实现 bind_tools 的 ChatOllama；
# langchain-community 里的同名类没有实现该方法，会导致 create_agent 报 NotImplementedError。
langchain>=1.0.0
langchain-core>=1.0.0
langchain-ollama>=1.0.0
langchain-mcp-adapters>=0.1.0

# 可选：使用 DeepSeek 云端模型时需要（本地 Ollama 模型不需要）。
# 装上之后在应用「设置」里填入 DEEPSEEK_API_KEY 即可选用。
# 不装也能跑，只是 DeepSeek 选项会提示需要安装。
langchain-openai>=1.0.0